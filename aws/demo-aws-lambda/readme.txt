This example creates the following resources
- iam role with policy to be execute the lambda function
- iam policy for logging. It will let us to see the logs using cloudwaths
- data defining the path of index.py
- lambda function

  
